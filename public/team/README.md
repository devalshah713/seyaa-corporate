Portrait for the leadership section on /company.

Drop the file in as `shaival-gandhi.jpg`. The page checks for it and renders an
"SG" monogram until it is there, so a missing file is never a broken image.

Aim for a portrait crop (roughly 4:5), shoulders up, shot against a plain or
quiet background. The page renders it with `object-cover`, so anything much
wider than tall will crop the sides.
